// Local development placeholder. After `terraform apply`, the deployed copy
// of this file in S3 is generated from config.template.js with the real API
// Gateway URL — this file is only used when serving the frontend locally.
window.APP_CONFIG = {
  apiBaseUrl: "", // e.g. "https://abc123.execute-api.us-east-1.amazonaws.com"
};
